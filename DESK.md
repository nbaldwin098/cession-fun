# Desk token — one person

Set `CESSION_DESK_TOKEN` on Render. Only Nicholas holds it.
Rotate if anyone else sees it. Never commit it. Never put it in chat.
Desk cannot withdraw. Desk can only read tickets and mark them.
